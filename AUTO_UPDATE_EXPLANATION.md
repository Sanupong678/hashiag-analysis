# การอัปเดตข้อมูล Trending Stock Tickers

## 📊 ข้อมูลจะเปลี่ยนหรือไม่?

### ✅ **ข้อมูลจะเปลี่ยนอัตโนมัติ** เมื่อ:

1. **มี Posts ใหม่ใน Database**
   - เมื่อมี posts ใหม่ที่มี $SYMBOL format
   - ระบบจะนับ mentions ใหม่
   - Trending tickers จะอัปเดตตามข้อมูลใหม่

2. **Frontend Auto-Refresh**
   - หน้า Home จะ refresh อัตโนมัติทุก **1 นาที**
   - ดึงข้อมูลใหม่จาก API
   - แสดง trending tickers ที่อัปเดตแล้ว

3. **เปลี่ยน Time Range**
   - เมื่อเปลี่ยน time range (1h, 6h, 24h, 7d, 30d)
   - ระบบจะดึงข้อมูลในช่วงเวลาที่เลือก
   - Trending tickers จะเปลี่ยนตามช่วงเวลา

### ⚠️ **ข้อมูลจะเหมือนเดิม** เมื่อ:

1. **ไม่มี Posts ใหม่**
   - ถ้าไม่มี posts ใหม่ใน database
   - Trending tickers จะแสดงข้อมูลเดิม
   - จำนวน mentions จะไม่เปลี่ยน

2. **Database ไม่ได้อัปเดต**
   - ถ้าไม่ได้ fetch posts ใหม่
   - ข้อมูลจะ fix ตามข้อมูลที่มี

## 🔄 วิธีทำให้ข้อมูลอัปเดตอัตโนมัติ

### วิธีที่ 1: รัน app.py (แนะนำ - รวม scheduler แล้ว)

**แค่รัน `app.py` ไฟล์เดียวก็พอ!** Scheduler จะทำงานอัตโนมัติใน background:

```bash
cd backend
python app.py
```

`app.py` จะ:
- ✅ เริ่ม Flask server (API)
- ✅ เริ่ม Background Scheduler อัตโนมัติ
- ✅ Fetch posts สำหรับ popular tickers (AAPL, TSLA, NVDA, etc.) ทุก 1 ชั่วโมง
- ✅ อัปเดต database อัตโนมัติ

**ไม่ต้องรัน `scheduler.py` แยก!**

### วิธีที่ 2: รัน Scheduler แยก (ถ้าต้องการ)

ถ้าต้องการรัน scheduler แยก (ไม่แนะนำ):

```bash
cd backend
python scheduler.py
```

### วิธีที่ 2: Manual Fetch

Fetch posts ด้วยตัวเอง:

```bash
cd backend
python fetch_reddit.py
```

หรือเรียก API:
```
GET http://localhost:5000/api/hashtags?keyword=$AAPL
```

### วิธีที่ 3: ใช้ Cron Job (Production)

สำหรับ production server, ใช้ cron job:

```bash
# Edit crontab
crontab -e

# Add this line to fetch every hour
0 * * * * cd /path/to/backend && python scheduler.py
```

## 📈 ตัวอย่างการทำงาน

### วันนี้ 10:00 AM
- Database มี posts: $AAPL (50 mentions), $TSLA (30 mentions)
- Trending: 1. $AAPL (50), 2. $TSLA (30)

### วันนี้ 11:00 AM (หลัง scheduler fetch)
- มี posts ใหม่: $AAPL (20 mentions ใหม่), $NVDA (40 mentions ใหม่)
- Database อัปเดต: $AAPL (70), $TSLA (30), $NVDA (40)
- Trending: 1. $AAPL (70), 2. $NVDA (40), 3. $TSLA (30)

### วันนี้ 11:01 AM (frontend auto-refresh)
- Frontend refresh อัตโนมัติ
- แสดง trending tickers ที่อัปเดตแล้ว

## ⚙️ การตั้งค่า

### ปรับ Auto-Refresh Interval

แก้ไขใน `frontend/public/js/home.js`:

```javascript
// เปลี่ยนจาก 60000 (1 นาที) เป็นค่าอื่น
setInterval(loadHomeData, 300000); // 5 นาที
```

### ปรับ Scheduler Interval

แก้ไขใน `backend/scheduler.py`:

```python
# เปลี่ยนจาก 1 hour เป็นค่าอื่น
schedule.every(30).minutes.do(fetch_stock_posts)  # ทุก 30 นาที
schedule.every(2).hours.do(fetch_stock_posts)      # ทุก 2 ชั่วโมง
```

### เพิ่ม Tickers ที่ต้องการ Monitor

แก้ไขใน `backend/scheduler.py`:

```python
POPULAR_TICKERS = ['AAPL', 'TSLA', 'NVDA', 'MSFT', 'AMZN', 'GOOGL', 'META', 'AMD', 'NFLX', 'SPY', 'GME', 'AMC']
```

## 🎯 สรุป

- ✅ **ข้อมูลจะเปลี่ยน** เมื่อมี posts ใหม่
- ✅ **Frontend auto-refresh** ทุก 1 นาที
- ✅ **Scheduler auto-fetch** ทุก 1 ชั่วโมง (ถ้ารัน scheduler)
- ⚠️ **ข้อมูลจะ fix** ถ้าไม่มี posts ใหม่

**แนะนำ**: รัน `scheduler.py` เพื่อให้ข้อมูลอัปเดตอัตโนมัติ!

