# 📋 คำอธิบาย: Real-time Mode และ Price Fetching

## ✅ การแก้ไขที่ทำแล้ว

### 1. **การดึงราคาหุ้น (Price Fetching) - Real-time เสมอ**
- ✅ **แก้ไขแล้ว:** Frontend จะดึงราคาหุ้นแบบ **real-time เสมอ** (ไม่ว่าจะเปิดหรือปิด real-time mode)
- ✅ ใช้ `?realtime=true` parameter เมื่อเรียก `/api/stock/{ticker}/info`
- ✅ ราคาหุ้นจะถูกอัปเดตแบบ real-time ทุกครั้งที่แสดงผล

### 2. **Database Mode (ปิด Real-time)**
- ✅ **ดึงข้อมูลจาก:** `post_reddit` และ `post_yahoo` collections ใน database
- ✅ **ใช้ endpoint:** `/api/trending-topics`
- ✅ **ราคาหุ้น:** ดึงแบบ real-time เสมอ (ไม่ขึ้นกับ real-time mode toggle)
- ✅ **ข้อมูล posts:** ใช้ข้อมูลจาก database ที่ถูกอัปเดตอัตโนมัติ:
  - Reddit: ทุก 5 นาที (โดย Reddit Bulk Scheduler)
  - Yahoo Finance: ทุก 2 ชั่วโมง (โดย Scheduled Updater)
- ✅ **ข้อดี:** เร็วกว่า, ไม่ต้องรอ API, ข้อมูลพร้อมใช้งานทันที

### 3. **Real-time Mode (เปิด Real-time)**
- ✅ **ดึงข้อมูลจาก:** 
  1. Database (`stocks` collection) - ข้อมูลที่ถูก aggregate แล้ว
  2. **API (Reddit, Twitter/X)** - ดึง post ใหม่เพื่อวิเคราะห์ sentiment เพิ่มเติม
- ✅ **ใช้ endpoint:** `/api/trending-realtime`
- ✅ **ราคาหุ้น:** ดึงแบบ real-time เสมอ (เหมือน Database Mode)
- ✅ **ข้อมูล posts:** 
  - ใช้ข้อมูลจาก database (เหมือน Database Mode)
  - **+ ดึง post ใหม่จาก Reddit API** สำหรับ top 10 trending tickers
  - **+ ดึง post ใหม่จาก Twitter/X API** (ถ้ามี API key) สำหรับ top 5 trending tickers
  - วิเคราะห์ sentiment จาก post ใหม่และรวมเข้ากับข้อมูลจาก database
- ✅ **ข้อดี:** ข้อมูลใหม่กว่า, มี sentiment analysis ที่อัปเดตจาก post ล่าสุด

## 🔄 การทำงานของ Scheduled Updater

### Reddit Bulk Scheduler (ทุก 5 นาที)
- **ทำงาน:** ดึงข้อมูล Reddit posts จาก subreddits ต่างๆ
- **บันทึกที่:** `post_reddit` collection
- **ผลต่อ Database Mode:** ✅ **มีผล** - ข้อมูลจะถูกอัปเดตใน database อัตโนมัติ
- **ผลต่อ Real-time Mode:** ✅ **มีผล** - แต่ real-time mode จะดึง post ใหม่เพิ่มเติมจาก API

### Scheduled Updater (Yahoo Finance ทุก 2 ชั่วโมง)
- **ทำงาน:** ดึงข้อมูลข่าวจาก Yahoo Finance
- **บันทึกที่:** `post_yahoo` collection และ `stocks` collection
- **ผลต่อ Database Mode:** ✅ **มีผล** - ข้อมูลจะถูกอัปเดตใน database อัตโนมัติ
- **ผลต่อ Real-time Mode:** ✅ **มีผล** - แต่ real-time mode จะดึง post ใหม่เพิ่มเติมจาก API

## 📊 สรุปความแตกต่าง

| Feature | Database Mode (ปิด) | Real-time Mode (เปิด) |
|---------|-------------------|---------------------|
| **ราคาหุ้น** | ✅ Real-time เสมอ | ✅ Real-time เสมอ |
| **ข้อมูล Posts** | จาก Database | จาก Database + API (Reddit, Twitter) |
| **Sentiment Analysis** | จาก Database | จาก Database + Post ใหม่จาก API |
| **ความเร็ว** | ⚡ เร็ว (ไม่ต้องรอ API) | 🐌 ช้ากว่า (ต้องรอ API) |
| **ข้อมูลใหม่** | ตาม Scheduled Updater | ใหม่กว่า (ดึง post ใหม่ทันที) |

## 💡 คำแนะนำการใช้งาน

### สำหรับการใช้งานปกติ:
- **ปิด Real-time Mode (Database Mode)**
  - ราคาหุ้น: Real-time เสมอ ✅
  - ข้อมูล posts: จาก database (อัปเดตอัตโนมัติ)
  - เร็วกว่าและเสถียรกว่า

### สำหรับข้อมูลใหม่ล่าสุด:
- **เปิด Real-time Mode**
  - ราคาหุ้น: Real-time เสมอ ✅
  - ข้อมูล posts: จาก database + ดึง post ใหม่จาก API
  - Sentiment: รวมจาก database และ post ใหม่
  - ข้อมูลใหม่กว่าแต่ช้ากว่า

## 🔍 ตรวจสอบการทำงาน

### ดูจาก Backend Logs:
```
🔄 [REAL-TIME MODE] Fetching trending data from database + API (source: all)
   ✅ Found 1000 stocks in database
   🔄 Fetching new posts from API for top 20 trending tickers...
   📊 AAPL: Added 10 real-time posts, 10 new sentiment scores
   ✅ Fetched 150 new posts from API
✅ [REAL-TIME MODE] Data aggregation complete:
   Total tickers found: 947
   ✅ Real-time posts fetched: 150 posts from API
```

### ดูจาก Frontend Console:
```
💰 Fetching prices for 50 tickers without price data...
✅ Fetched stock prices for 50 tickers
🔄 Re-rendering word cloud with price data...
```

## 📝 สรุป

**การดึงราคาหุ้น:**
- ✅ **Real-time เสมอ** (ไม่ว่าจะเปิดหรือปิด real-time mode)
- ✅ ใช้ `?realtime=true` parameter

**Database Mode (ปิด Real-time):**
- ✅ ใช้ข้อมูลจาก database ที่ถูกอัปเดตอัตโนมัติ
- ✅ ราคาหุ้น: Real-time
- ✅ เร็วกว่าและเสถียรกว่า

**Real-time Mode (เปิด Real-time):**
- ✅ ใช้ข้อมูลจาก database + ดึง post ใหม่จาก API
- ✅ ราคาหุ้น: Real-time
- ✅ วิเคราะห์ sentiment จาก post ใหม่
- ✅ ข้อมูลใหม่กว่าแต่ช้ากว่า
